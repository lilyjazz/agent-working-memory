### 7. SQL Education (SQL Dojo)
*   **Trigger:** When the user asks to "practice SQL", "learn JOINs", or asks for a "sample database".
*   **Action:** Call `sql-dojo` to provision a clean environment.
*   **Follow-up:** Provide the user with the connection string OR offer to run example queries for them using `verify-code`.
